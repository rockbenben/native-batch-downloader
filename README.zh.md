# Native Batch Downloader

> 365 开源计划 #001 · 通过浏览器原生通道批量下载文件的 Chrome 扩展

把一堆链接粘进去，一次性全部下载。因为它走的是浏览器自己的下载通道——和你手动点链接下载是同一条路——所以**需要登录才能下的文件也能下**，登录状态自动带上，无需任何配置。

中文 | [English](README.md)

[![Chrome Web Store](https://img.shields.io/chrome-web-store/v/fmmihnoplefjcfoggfgpeiomlbmjnfpm)](https://chromewebstore.google.com/detail/native-batch-downloader/fmmihnoplefjcfoggfgpeiomlbmjnfpm)

![粘贴链接，一次全部下载——需要登录的文件也能下](assets/banner.png)

## 工作原理

点击工具栏图标，把链接粘进去（一行一个），设好间隔和并发数，点 **开始下载** 就行。

简单说，凡是你在这个浏览器里点链接能下载的东西，这里都能批量下，而且会自动保持登录态。下载由浏览器本身完成，就像你亲手把每个链接都点了一遍，所以登录状态始终有效。

下载队列在后台持续运行，即使关闭弹窗也会继续下载——随时重新打开即可看到实时进度。

## 功能特性

- **需要登录的文件也能下** -- 文件走浏览器自己的下载通道，登录状态自动带上。凡是你登录后能手动下载的，这里都能批量下。
- **并发与间隔控制** -- 可设置并行下载数量（1-200）及每次开始下载之间的最小间隔（毫秒）——包括首批在内的每一次启动都按间隔错开，不会对限速服务器瞬间打满并发。
- **记住你的设置** -- 并发、间隔和子文件夹开关跨会话保留（随浏览器账号同步）。
- **徽章进度** -- 批量任务运行时工具栏图标显示剩余数量，关闭弹窗也能看到进度。
- **失败重试** -- 任务结束后一键把所有失败、中断或被停止丢弃的链接重新入队。
- **拖入列表文件** -- 把装有链接的 `.txt`/`.csv` 文件拖到弹窗上即可载入编辑器。
- **整洁的子文件夹** -- 文件保存到下载目录的 `BatchDL/<日期>/` 中（可关闭），200 个文件的批次不会淹没其他下载。
- **实时进度** -- 总计 / 成功 / 失败 / 等待 四项实时统计，附带进度条。
- **不限文件格式** -- PDF、图片、视频、压缩包、可执行文件——只要是直链，什么都能下。
- **后台持续下载** -- 关闭弹窗不会中断队列；随时重新打开即可继续查看实时进度。
- **智能去重** -- 重复链接只下载一次，并在日志中提示跳过的无效/重复行数。
- **文件名省心** -- 自动保留合适的文件名，无论名字来自链接本身还是服务器。
- **24 种语言** -- AR、BN、DE、EN、ES、FA、FIL、FR、HI、ID、IT、JA、KO、NL、PL、PT-BR、RU、TH、TR、UK、UR、VI、ZH-CN、ZH-TW。

## 安装方法

需要 Chrome 110 或更高版本（已在 manifest 中声明，应用商店不会向更低版本分发）。

**从 Chrome 应用商店安装（推荐）：**

直接前往 [Chrome 应用商店](https://chromewebstore.google.com/detail/native-batch-downloader/fmmihnoplefjcfoggfgpeiomlbmjnfpm) 安装。

**手动安装（开发用）：**

1. 下载或克隆本仓库。
2. 在 Chrome 中打开 `chrome://extensions/`。
3. 打开右上角的 **开发者模式**。
4. 点击 **加载已解压的扩展程序**，选择本项目文件夹。
5. 工具栏出现扩展图标，点击即可打开弹窗。

## 使用方法

![粘贴或拖入链接，实时查看进度，一键重试失败项](assets/popup-states.png)

1. 点击工具栏中的扩展图标。
2. 在文本框中粘贴下载直链，**每行一个**——或把装有链接的 `.txt`/`.csv` 文件直接拖进弹窗。开始按钮上会实时显示识别到的有效链接数。没点开始就关掉弹窗也不会丢——重新打开列表还在。
3. 根据需要调整 **并发数**（默认 10）和 **间隔**（默认 0 ms），并选择是否 **存入子文件夹**（默认开启，文件归入 `BatchDL/<日期>/`）。三项设置都会自动记住。
4. 点击 **开始下载**（或在文本框中按 **Ctrl/Cmd+Enter**）。
5. 实时查看日志和统计信息。可关闭弹窗，下载会继续，工具栏图标上的徽章会显示剩余数量。点击 **停止** 可取消任务（含正在进行的下载）。
6. 任务结束后若有失败项，会出现 **重试失败项** 按钮——一键把这些链接重新入队。

## 注意事项

### URL 必须是直链

如果一个网站是点按钮后通过 JS 动态生成临时下载地址的（比如网盘的"点击下载"），粘贴页面 URL 进去是没用的，需要拿到实际的文件直链。

### 依赖当前浏览器的登录态

能自动带 Cookie 是因为走的是浏览器自己的请求通道，前提是你在浏览器里已经登录了目标网站。如果目标站需要登录但你没登录，该 403 还是 403。

### Chrome 同域名并发限制

Chrome 对**同一个网站**同时只传约 **6 个文件**，所以即使你设并发 100，同站下载实际同时在传的也就 6 个左右，其余排队等候。从多个不同网站下载则没有这个瓶颈。

### 危险文件类型会触发 Chrome 警告

`.exe`、`.bat` 等可执行文件可能触发 Chrome 内置安全提示，会打断自动下载流程。任何扩展都无法绕过这一限制——这是 Chrome 的安全规则。

**解决办法**：打开 `chrome://settings/security`，将安全浏览等级设为 **无保护**，下完后记得改回来。

PDF 等常规文档类型完全不受影响，直接用就行。

### 大间隔 + 关闭弹窗

扩展的后台进程在空闲一段时间后可能被 Chrome 暂停。如果你设了较大的 **间隔**（比如好几秒）并关闭了弹窗，暂停可能正好落在等待间隙——没有正在进行的下载来唤醒它，任务会静默停住。重新打开弹窗即可自动恢复。如果保持弹窗打开、或把间隔设为 0，则不会出现这种情况。

## 开发说明

本扩展无构建步骤——纯 HTML/CSS/JS，直接「加载已解压的扩展程序」即可。

- 修改任何语言的 `title`/`start`/`stop`/`retryFailed` 消息或新增语言后，需重新生成打包的展示字体子集：先 `pip install fonttools brotli`，再 `python scripts/subset-display-fonts.py`（缺字会直接报错）。
- 生成商店上传包：`python scripts/package.py`

## 关于 365 开源计划

本项目是 [365 开源计划](https://github.com/rockbenben/365opensource) 的第 001 个项目。

一个人 + AI，一年 300+ 个开源项目。[提交你的需求 →](https://my.feishu.cn/share/base/form/shrcnI6y7rrmlSjbzkYXh6sjmzb)

## 许可证

[MIT](LICENSE)
